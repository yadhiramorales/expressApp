<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link href="/css/styles.css" rel="stylesheet">
    <title>Album Results</title>
</head>
<body>
    <h1 class="text-center bg-black p-4 text-white m-0">Album Results:</h1>
    <%- include("partials/navbar.ejs") %>

    <div class="container mt-4">
        <% if (!albums || albums.length === 0) { %>
        <p>No exact matches. Try the EXACT album title (spacing/case sensitive).</p>
        <% } else { %>
        <div class="row row-cols-1 g-3">
            <% albums.forEach(a => { %>
                <div class="col">
                    <div class="card shadow-sm p-3">
                        <h5 class="card-title"><%= a.title %></h5>
                        <p class="mb-1"><strong>Artist:</strong>
                        <%= (a['artist-credit'] || []).map(ac => ac.name).join(', ') || 'Unknown' %>
                        </p>
                        <p class="mb-0"><strong>Release date:</strong>
                        <%= a['first-release-date'] || 'N/A' %>
                        </p>
                    </div>
                </div>
            <% }) %>
        </div>
        <% } %>
    </div>
</body>
</html>